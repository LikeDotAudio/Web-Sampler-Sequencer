{
  "name": "web-sampler-sequencer",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for SAMPLER.LIKE.AUDIO — compiles libControl/**/*.jsx into dist/app.js",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check"
  },
  "devDependencies": {
    "@babel/core": "^7.28.4",
    "@babel/preset-react": "^7.27.1"
  }
}
